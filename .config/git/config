# Git configuration

[include]
    path = ~/.config/git/delta-themes.gitconfig

[core]
    pager = delta

[interactive]
    diffFilter = delta --color-only

[delta]
    navigate = true
    features = bex-codes

[merge]
    conflictstyle = diff3

[diff]
    colorMoved = default
